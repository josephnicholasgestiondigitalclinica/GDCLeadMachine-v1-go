[install]
# Install Python dependencies for the backend
cmd = "pip install -r requirements.txt"

[build]
# Build the frontend
cmd = "npm ci && CI=false npm run build"

[start]
# Start the backend server
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
